package com.example.webshop; // Adjust the package name accordingly

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication // This annotation indicates that this is a Spring Boot application
public class WebshopApplication {

    public static void main(String[] args) {
        SpringApplication.run(WebshopApplication.class, args); // Starts the Spring application
    }
}
